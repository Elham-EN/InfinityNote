import React, { ReactElement } from "react";

function WorkspacePage({ params }: { params: { workspaceid: string } }): ReactElement {
  return <div className="relative">WorkspacePage</div>;
}

export default WorkspacePage;
